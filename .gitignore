# --- Secrets & Credentials (CRITICAL) ---
# Spotify OAuth tokens — contains access_token and refresh_token
.spotify-token.json
spotify-token.json
*token*.json

# User config — contains client_id, client_secret, playlist IDs
*/config.yaml
credentials.yaml

# --- State & Runtime Data ---
state.json
*.state
cursors.json

# Cron logs
*/log.txt
*log.txt